# 개발(quasar dev)용 API 주소 - 로컬 quasar-server
QCLI_API_BASE_URL="http://localhost:8081/api"

# 운영 빌드(quasar build)는 기본적으로 같은 도메인의 /api를 호출한다 (CORS 불필요).
# 운영 API가 사이트와 다른 도메인일 때만 아래 주석을 해제해서 지정.
# QCLI_API_PROD_URL="https://api.innoweaver.net/api"
